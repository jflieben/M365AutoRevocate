name: Release

# Cuts a GitHub release automatically whenever the VERSION file changes on main
# and no release yet exists for that version. The release carries the packaged
# function code, the web app, checksums and the bootstrap installer, so the
# one-line installer always pulls a real, tested build.

on:
  push:
    branches: [ main ]
    paths: [ 'VERSION' ]

permissions:
  contents: write

jobs:
  # Reuse the exact analyze + test gate from CI so we never release a broken build.
  test:
    uses: ./.github/workflows/ci.yml

  release:
    name: Package + publish release
    needs: test
    runs-on: ubuntu-latest
    env:
      GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
    steps:
      - uses: actions/checkout@v4

      - name: Read version and check for an existing release
        id: ver
        shell: pwsh
        run: |
          $version = (Get-Content ./VERSION -Raw).Trim()
          if ($version -notmatch '^\d+(\.\d+){0,3}$') { throw "VERSION '$version' is not a valid version." }
          $tag = "v$version"
          gh release view $tag *> $null
          $exists = ($LASTEXITCODE -eq 0)
          "version=$version" >> $env:GITHUB_OUTPUT
          "tag=$tag"         >> $env:GITHUB_OUTPUT
          "exists=$exists"   >> $env:GITHUB_OUTPUT
          if ($exists) { Write-Host "Release $tag already exists; nothing to do." }
          else { Write-Host "VERSION is $version; will create release $tag." }

      - name: Build artifacts
        if: steps.ver.outputs.exists == 'False'
        shell: pwsh
        run: |
          $version = '${{ steps.ver.outputs.version }}'
          New-Item -ItemType Directory -Force out | Out-Null
          # Function code (exclude any local settings that could hold secrets).
          $stage = Join-Path ([System.IO.Path]::GetTempPath()) 'src'
          Copy-Item ./src $stage -Recurse -Force
          Get-ChildItem $stage -Filter 'local.settings.json*' -Recurse -Force | Remove-Item -Force
          Compress-Archive -Path (Join-Path $stage '*') -DestinationPath "out/M365AutoRevocate-src-$version.zip" -Force
          Compress-Archive -Path ./web/* -DestinationPath "out/M365AutoRevocate-web-$version.zip" -Force
          Copy-Item ./install.ps1 out/install.ps1
          # Checksums for the zips.
          Get-ChildItem out -Filter *.zip | ForEach-Object {
            $h = (Get-FileHash $_.FullName -Algorithm SHA256).Hash
            "$h  $($_.Name)" | Out-File "out/$($_.Name).sha256" -Encoding ascii
          }
          Get-ChildItem out

      - name: Extract release notes from CHANGELOG
        if: steps.ver.outputs.exists == 'False'
        shell: pwsh
        run: |
          $version = '${{ steps.ver.outputs.version }}'
          $notes = ''
          if (Test-Path ./CHANGELOG.md) {
            $lines = Get-Content ./CHANGELOG.md
            # Prefer a heading naming this version; otherwise the Unreleased section.
            $start = -1
            for ($i = 0; $i -lt $lines.Count; $i++) {
              if ($lines[$i] -match '^##\s' -and ($lines[$i] -match [regex]::Escape($version) -or $lines[$i] -match '(?i)unreleased')) { $start = $i; break }
            }
            if ($start -ge 0) {
              $buf = @()
              for ($j = $start + 1; $j -lt $lines.Count; $j++) {
                if ($lines[$j] -match '^##\s') { break }
                $buf += $lines[$j]
              }
              $notes = ($buf -join "`n").Trim()
            }
          }
          if (-not $notes) { $notes = "Release $version." }
          $installCmd = 'iex (irm https://raw.githubusercontent.com/${{ github.repository }}/main/install.ps1)'
          $notes = "$notes`n`n---`n**Install / update (Azure Cloud Shell, PowerShell):**`n`n``````powershell`n$installCmd`n``````"
          $notes | Out-File release-notes.md -Encoding utf8
          Write-Host '----- release notes -----'
          Write-Host $notes

      - name: Create the release
        if: steps.ver.outputs.exists == 'False'
        shell: pwsh
        run: |
          $assets = Get-ChildItem out | ForEach-Object { $_.FullName }
          $ghArgs = @(
            '${{ steps.ver.outputs.tag }}',
            '--title', 'M365AutoRevocate ${{ steps.ver.outputs.version }}',
            '--notes-file', 'release-notes.md',
            '--target', '${{ github.sha }}'
          ) + $assets
          gh release create @ghArgs
          Write-Host "Published ${{ steps.ver.outputs.tag }}."
